Option A → Add inter-service communication
           (Transaction Service calls User Service 
            to validate sender/receiver exist)

Option B → Add validation & exception handling
           (GlobalExceptionHandler, custom errors)

Option C → Add security (Spring Security + JWT)

Option D → Add Kafka event streaming
           (Publish transaction events)

Option E → Add a new service
           (Notification, Account, Audit)
